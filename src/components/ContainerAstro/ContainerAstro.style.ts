import styled from "styled-components";

export const ContainerAstroElement = styled.div`

&.primary {

  position: absolute;
  z-index: 20;
  width: 300px; 
  margin-left: 50px;
  margin-top: 50px;
}

  &.right {
    right: 0;
  }
  &.left {
    left: 0;
  }
`